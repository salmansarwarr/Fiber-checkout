// ─── Channel types ───────────────────────────────────────────────────────────
// Re-exported from @nervosnetwork/fiber-js — the canonical type source.

export type {
    Channel,
    ChannelState,
    ListChannelsParams,
    ListChannelsResult,
    OpenChannelParams,
    OpenChannelResult,
    AcceptChannelParams,
    AcceptChannelResult,
    AbandonChannelParams,
    ShutdownChannelParams,
    UpdateChannelParams,
} from "@nervosnetwork/fiber-js";
